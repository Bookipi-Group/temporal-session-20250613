export const sendReminderEmail = async (email: string, message: string) => {
  // todo: Implement the logic to send an email
  console.log(`Sending reminder email to ${email} with message: ${message}`);
};
